-- MySql
CREATE TABLE items (  
  id varchar(40),
  name varchar(50) NOT NULL,
  brand varchar(50),
  createdAtTime bigint NOT NULL,
  description varchar(256), 
  PRIMARY KEY (id)
);

CREATE TABLE tags (
  id varchar(40),
  tag varchar(50), 
  PRIMARY KEY (id, tag)
);
